namespace Launcher.Core
{
    internal static class Secrets
    {
        // To get your own key create an Application on Microsoft Azure.
        // When doing so set the redirect url to "http://localhost"

        //Application (client) ID
        internal const string AzureApp_ClientId = "";
    }
}
